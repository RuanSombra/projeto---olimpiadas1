import 'package:flutter/material.dart';

class PageTravel extends StatelessWidget {
  const PageTravel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
